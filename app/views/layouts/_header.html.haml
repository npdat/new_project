:javascript
  $(document).ready(function () {
    $(".dropdown").hover(

        function() {
            $('.dropdown-menu', this).not('.in .dropdown-menu').stop( true, true ).slideDown("fast");
            $(this).toggleClass('open');
        },
        function() {
            $('.dropdown-menu', this).not('.in .dropdown-menu').stop( true, true ).slideUp("fast");
            $(this).toggleClass('open');
        }
    );
  });
.navbar.navbar-default.navbar-fixed-top{:role => "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{"data-target" => "#navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.pull-left{:href => "#"}
        =image_tag("wallem-logo.png", class: "brand", style: "margin-top: 2px; height:45px")
        %span.icon-bar
      %a.navbar-brand{:href => "#"} &nbsp;&nbsp;Ok to Board
    .collapse.navbar-collapse#navbar-collapse-1
      %ul.nav.navbar-nav.navbar-right
        %li
          =link_to 'Create', batches_create_path
        %li
          =link_to 'List', boarding_documents_list_url
        %li
          =link_to 'Approval', batches_approval_path
        %li
          =link_to 'Need Conform', batches_need_confirm_path
        - if current_user
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              Hi,
              = current_user.email
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li
                = link_to 'Sign out', destroy_user_session_path, method: :delete
              - if current_user
                - if current_user.email == 'admin@tma.com.vn'
                  %li
                    = link_to 'Admin', '/admin'
                  %li
                    = link_to 'Manage Users', '/manage_users'

